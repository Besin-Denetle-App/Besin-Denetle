# Server
PORT=3200

# Database (Docker compose ile uyumlu değerler)
DB_HOST=localhost
DB_PORT=5432
DB_USER=myuser
DB_PASSWORD=mypassword
DB_NAME=besindenetle

# AI (boş bırakılırsa mock mod aktif olur)
GEMINI_API_KEY=

# Auth (boş bırakılırsa mock mod aktif olur)
MOCK_AUTH=true

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production
