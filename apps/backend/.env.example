# Database Environment Variables
# PostgreSQL veritabanı bağlantı ayarları (Docker compose ile kullanım için)

# Veritabanı host (Docker container adı veya localhost)
DB_HOST=localhost

# Veritabanı port
DB_PORT=5432

# Kullanıcı adı (docker-compose.yml ile aynı olmalı)
DB_USER=myuser

# Şifre (docker-compose.yml ile aynı olmalı)
DB_PASSWORD=mypassword

# Veritabanı adı (docker-compose.yml ile aynı olmalı)
DB_NAME=besindenetle

# Node ortamı (development, production, test)
NODE_ENV=development
