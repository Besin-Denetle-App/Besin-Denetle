# =================================================================
# Environment Variables - Besin Denetle Mobile
# =================================================================
# Bu dosyayı .env olarak kopyalayın: cp .env.example .env

# =================================================================
# DEVELOPMENT (expo start) - ZORUNLU
# =================================================================
# Local geliştirme için bu değişkenler ZORUNLUDUR

# Backend sunucusu (bilgisayarınızın IP adresi)
DEV_API_HOST=192.168.1.106
DEV_API_PORT=3200

# Google OAuth (expo start için de gerekli!)
GOOGLE_WEB_CLIENT_ID=xxx.apps.googleusercontent.com
GOOGLE_ANDROID_CLIENT_ID=xxx.apps.googleusercontent.com
GOOGLE_IOS_CLIENT_ID=xxx.apps.googleusercontent.com

# =================================================================
# LOCAL BUILD (eas build --local) - OPSİYONEL
# =================================================================
# Windows'ta local build desteklenmiyor (macOS/Linux gerekli).
# Eğer local build alacaksanız, aşağıdakileri yorum satırından çıkarın.
# Cloud build (eas build) için bu değerler EAS Secrets'tan gelir.

# --- Preview Build için (--profile preview --local) ---
API_HOST=192.168.1.106
API_PORT=3200

# --- Production Build için (--profile production --local) ---
API_URL=https://api.besindenetle.app/api

# =================================================================
# ÖZET
# =================================================================
#
# | Senaryo              | Gerekli Değişkenler                    |
# |----------------------|----------------------------------------|
# | expo start           | DEV_API_*, GOOGLE_*                    |
# | eas build (cloud)    | Hiçbiri (EAS Secrets'tan gelir)        |
# | eas build --local    | API_HOST/PORT veya API_URL + GOOGLE_*  |
