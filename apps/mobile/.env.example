# =================================================================
# Environment Variables - Besin Denetle Mobile
# =================================================================
# Bu dosyayı .env olarak kopyalayın: cp .env.example .env
#
# ⚠️ ÖNEMLİ: Bu dosya SADECE "expo start" için kullanılır!
# Build için değişkenler eas.json dosyasında tanımlıdır.
# =================================================================

# =================================================================
# DEVELOPMENT - expo start (ZORUNLU)
# =================================================================
# Local geliştirme sunucusuna bağlanmak için

DEV_API_HOST=192.168.1.106
DEV_API_PORT=50101

# Google OAuth Client ID'leri
GOOGLE_WEB_CLIENT_ID=xxx.apps.googleusercontent.com
GOOGLE_ANDROID_CLIENT_ID=xxx.apps.googleusercontent.com
GOOGLE_IOS_CLIENT_ID=xxx.apps.googleusercontent.com

# =================================================================
# BUILD KONFİGÜRASYON REHBERİ
# =================================================================
#
# ┌─────────────────┬─────────────────┬────────────────────────────┐
# │ Senaryo         │ Env Kaynağı     │ API Değişkenleri           │
# ├─────────────────┼─────────────────┼────────────────────────────┤
# │ expo start      │ .env dosyası    │ DEV_API_HOST:DEV_API_PORT  │
# │ local preview   │ eas.json        │ API_URL                    │
# │ local prod      │ eas.json        │ API_URL                    │
# │ cloud preview   │ eas.json        │ API_URL                    │
# │ cloud prod      │ EAS Secrets     │ API_URL                    │
# └─────────────────┴─────────────────┴────────────────────────────┘
#
# Local build için: eas.json > build > [profile] > env
# Cloud build için: EAS Secrets veya eas.json
#
# =================================================================
