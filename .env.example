# Ortam Ayarı
# development: Varsayılan değerler kullanılır, detaylı hata mesajları gösterilir
# production: Zorunlu değişkenler kontrol edilir (DB, JWT, OAuth), güvenlik aktif
NODE_ENV=development

# Server Ayarları
# Uygulamanın çalışacağı port.
# Not: 0 değeri verilirse işletim sistemi rastgele boş bir port atar.
PORT=50101

# Redis Ayarları
# Rate limiting için kullanılır.
REDIS_HOST=localhost
REDIS_PORT=50102

# Veritabanı Ayarları
# Docker Compose ortamı için varsayılan değerlerdir.
# Production'da zorunlu: DB_HOST, DB_USER, DB_PASSWORD, DB_NAME
DB_HOST=localhost
DB_PORT=50103
DB_USER=myuser
DB_PASSWORD=mypassword
DB_NAME=besindenetle

# Yapay Zeka Ayarları
# API anahtarı girilmezse servis "mock" modunda çalışır (sahte veri döner).
GEMINI_API_KEY=

# Hızlı model (Prompt 1-2): gemini-3-flash, gemini-3-flash-preview, gemini-2.5-flash
# Akıllı model (Prompt 3): gemini-3-pro, gemini-3-pro-preview, gemini-2.5-pro
GEMINI_MODEL_FAST=gemini-3-flash-preview
GEMINI_MODEL_SMART=gemini-3-pro-preview

# Grounding stratejisi:
# SCHEMA: Structured Output (responseSchema ile) - Daha güvenilir JSON, RECITATION riski
# PROMPT: Prompt-based JSON - RECITATION'a dayanıklı, parse riski
GEMINI_GROUNDING_STRATEGY=PROMPT

# JWT (JSON Web Token) Ayarları
# Token güvenliği için kullanılan gizli anahtar.
# Production'da zorunlu! En az 32 karakter önerilir.
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Google OAuth
# Google Cloud Console'dan alınan Client ID
# Production'da en az bir OAuth provider (Google veya Apple) zorunlu!
GOOGLE_WEB_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Apple Sign In
# Apple Developer Console'dan alınan Client ID (Bundle ID)
APPLE_CLIENT_ID=com.yourcompany.yourapp
